[project]
name = "{{ project_name|replace(' ', '-')|replace('.', '-')|trim() }}"
version = "0.1.0"
description = "Add your description here"
requires-python = ">={{ python_version }}"
dependencies = []
